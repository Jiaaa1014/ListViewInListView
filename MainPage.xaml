<Page
    x:Class="DoubleDetails.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:DoubleDetails"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    mc:Ignorable="d">

    <Grid>
        <ListView ItemsSource="{x:Bind ParentList}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:ParentClass">
                    <ListView ItemsSource="{x:Bind Children}">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="local:ChildClass">
                                <TextBlock Text="{x:Bind Id}" />
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
